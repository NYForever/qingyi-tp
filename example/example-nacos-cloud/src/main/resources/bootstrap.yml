server:
  port: 8082

spring:
  cloud:
    nacos:
      config:
        prefix:
        file-extension: yml
        group: DEFAULT_GROUP
        server-addr:
        namespace:
        username:
        password:

